# 开发笔记

记录一些仍在开发过程中的系统，开发完成后归档至`design.md`

## 登录的2fa认证

现在是在登录的同时选填一个totp code，直接一个接口认证，计划调整为两步认证：

- 用户输入用户名、密码点击登录，调用接口1
- 服务端验证用户名和密码，根据用户是否开启2FA：
    - 未开启：返回登录结果
    - 已开启：
        - 返回mfa-token，前端让用户输入totp code，与该token一起调用接口2
        - 服务端验证mfa-token和totp code，返回登录结果
- 前端完成登录

mfa-token管理：有效期5分钟、允许尝试5次

## 开发计划

- 数据库备份/恢复功能优化：
    - 目前在备份时，是一条记录处理一次，能不能每页查询结果分类处理，甚至在查询阶段就分类？
    - 目前在恢复时，即使完全一致的数据库记录也会覆写一次，尝试优化这一点
- (web)为`note`编写淡入淡出特效
- (web)修改个人信息，添加随机生成totp密钥功能
