# Commands

`pkill -f unnamed_plan_server -10`

- `pkill`: ps | grep | kill
- `-f`: 给出的匹配字符串长度超过15，需要提供该参数强制执行
- `-10`/`-12`：kill默认发送结束信号，-10表示发送`SIGUSR1`信号（用户自定义信号1，-12表示自定义信号2）。
  通常需要编写代码响应这些信号。
- 本项目中，响应自定义信号1的代码功能为：立即备份；响应自定义信号2的代码功能为：恢复
